<h2>{{#if commerceType.id}}Editar{{else}}Nuevo{{/if}} Tipo de Comercio</h2>
<form action="{{#if commerceType.id}}/admin/commerce-type/edit/{{commerceType.id}}{{else}}/admin/commerce-type/new{{/if}}" method="POST" enctype="multipart/form-data">
  <div class="mb-3">
    <label for="name" class="form-label">Nombre</label>
    <input type="text" class="form-control" id="name" name="name" value="{{commerceType.name}}" required>
  </div>
  <div class="mb-3">
    <label for="description" class="form-label">Descripción</label>
    <textarea class="form-control" id="description" name="description" required>{{commerceType.description}}</textarea>
  </div>
  <div class="mb-3">
    <label for="file" class="form-label">Icono</label>
    <input type="file" class="form-control" id="file" name="file">
    {{#if commerceType.icon}}
      <img src="/uploads/{{commerceType.icon}}" alt="Icon" style="height: 100px; object-fit: cover;">
    {{/if}}
  </div>
  <button type="submit" class="btn btn-primary">Guardar</button>
  <a href="/admin/commerce-types" class="btn btn-secondary">Volver</a>
</form>